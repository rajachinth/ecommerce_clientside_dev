import { CasechangedirectiveDirective } from './casechangedirective.directive';

describe('CasechangedirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new CasechangedirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
